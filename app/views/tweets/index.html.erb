<h1>Tweets</h1>

<ul>
  <% @tweets.each do |tweet| %>
    <li>
      <strong><%= tweet.user.username %> - <%= tweet.user.display_name %></strong>
      <%= tweet.body %>
      <small><%= tweet.created_at.strftime("%d/%m/%Y %H:%M") %></small>
    </li>
  <% end %>

  <%= paginate @tweets %>

</ul>

